
name: mysql test

on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: whoami
      - name: mariadb preparatrion
        run: sudo apt install -y mariadb-server
      - name: secure install
        run: mysql_secure_installation

